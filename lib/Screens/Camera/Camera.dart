import 'package:flutter/material.dart';

class CameraTeste extends StatefulWidget {
  @override
  _CameraTesteState createState() => _CameraTesteState();
}

class _CameraTesteState extends State<CameraTeste> {
  @override

  Widget build(BuildContext context) {
    return FloatingActionButton(
      onPressed: (){},
    );
  }
}
